---
title: Fake Store Example
summary: End-to-end reference pipeline using Axiomatic Engine and dbt-first transformation for Fake Store API reporting.
sectionOrder:
  - Problem
  - Architecture
  - Data model
  - Transform logic
  - Data quality/tests
  - Results/insights
  - Trade-offs/lessons
capabilityMaturity:
  ingestion: implemented
  transformation: implemented
  semantic_mcp: planned
evidence:
  - type: code
    claim: End-to-end implementation overview and usage guide
    sourcePath: projects/fake_store/README.md
    status: implemented
  - type: code
    claim: Runtime orchestration entrypoint and resource-level load hints
    sourcePath: projects/fake_store/run_pipeline.py
    status: implemented
  - type: code
    claim: dbt model contracts and star schema tests
    sourcePath: projects/fake_store/dbt_project/models/gold/schema.yml
    status: implemented
  - type: code
    claim: Environment contract for local execution
    sourcePath: projects/fake_store/.env.example
    status: implemented
  - type: diagram
    claim: Planned semantic layer and MCP walkthrough artefacts
    sourcePath: projects/fake_store/README.md
    status: planned
reproducibility:
  - command: cd projects/fake_store && uv sync
    assumption: Python 3.10+ with uv installed; run from repository root.
  - command: cd projects/fake_store && uv run python run_pipeline.py --skip-transforms
    assumption: Copy .env from .env.example and configure AXIOMATIC_* values.
  - command: cd projects/fake_store && uv run python run_pipeline.py --run-transforms
    assumption: dbt profile and target settings are valid for your selected warehouse.
assumptions:
  - Environment values are loaded from local, non-committed files.
  - Warehouse credentials and dataset permissions are configured before transform runs.
limitations:
  - "Demonstration-focused: production scheduling and alerting are not included."
  - Semantic MCP integration remains planned and is not yet published in this example.
---

This case study demonstrates a repeatable reporting workflow from API-first source data without embedding business logic in dashboards.
It provides a clear handover between data engineering and BI delivery while keeping implementation evidence verifiable.

## At a glance

- **Problem**: API data was easy to access but hard to govern and rerun.
- **What was built**: A reusable ingestion and dbt pipeline with a reporting-ready star schema.
- **Why it matters**: Reporting can evolve without hiding logic in dashboards.
- **Technology**: Axiomatic Engine, dlt, dbt, warehouse modelling, and Power BI.

## Engine layer

The engine layer defines ingestion, rerun behaviour, and transform contracts in versioned code.

- Source extraction and rerun policies are defined in [`projects/fake_store/run_pipeline.py`](https://github.com/axiomatic-bi/axiomatic-engine/blob/main/projects/fake_store/run_pipeline.py).
- dbt contracts and tests are maintained in [`models/gold/schema.yml`](https://github.com/axiomatic-bi/axiomatic-engine/blob/main/projects/fake_store/dbt_project/models/gold/schema.yml).
- Local runtime assumptions are documented in [`projects/fake_store/.env.example`](https://github.com/axiomatic-bi/axiomatic-engine/blob/main/projects/fake_store/.env.example).

## Reporting layer

Reporting assets consume the same contract but are versioned separately from engine runtime code.

- The PBIX artefact demonstrates report implementation against the governed model.
- Reporting outputs are evidence of delivery, not a place for hidden transformation logic.

## Model decisions

Model choices prioritise explicit grain, predictable reruns, and auditable quality checks.

- Gold models follow a star schema centred on cart-item facts with conformed user, product, and date dimensions.
- Resource-level load hints define rerun semantics (`merge` for updates, `replace` for snapshots).
- dbt schema tests run in the same path as model execution to keep validation aligned with transforms.

## Outcomes and limits

This example is demonstration-focused but designed to show reproducible and verifiable delivery quality.

- Demonstrates deterministic reruns and reproducible outputs from documented commands.
- Provides a baseline for new source adapters or additional reporting consumers.
- Production scheduling and alerting are not included in this reference build.
