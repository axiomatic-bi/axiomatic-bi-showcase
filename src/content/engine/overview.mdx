---
title: Engine Overview
summary: Contract-first orchestration with typed settings, adapter isolation, and package-grade release discipline.
highlights:
  - title: Problem
    body: Pipeline concerns are often mixed, so reruns and ownership become hard to reason about.
  - title: What was built
    body: A contract-first engine with typed settings and adapter boundaries across runtime stages.
  - title: Current scope
    body: REST and file ingestion, Duck-compatible warehouses, and dbt-first transformation.
  - title: Why it matters
    body: Pipeline flow stays centralised while backend-specific behaviour remains isolated.
problemStatement: Data projects often blur orchestration, source logic, warehouse behaviour, and transformation execution. This increases maintenance cost and makes rerun behaviour harder to reason about. Axiomatic Engine addresses this by separating responsibilities with typed protocols and adapter boundaries.
architectureIntro:
  - The engine is organised into contracts, adapters, and core stages. Sources expose resources through protocol boundaries, ingestion is executed with dlt into a selected warehouse adapter, and transformation is delegated through a transformation adapter.
  - This keeps pipeline flow centralised while backend specifics remain isolated. Settings are loaded from AXIOMATIC_* variables with CLI override support.
strategySections:
  - heading: Core contracts
    body: Source, storage, warehouse, and transformation protocols define extension points. Literal kinds constrain available options, so unsupported backends fail explicitly instead of degrading silently. This helps keep project code domain-aware and engine code domain-agnostic.
  - heading: Execution flow
    body: "Runtime flow is consistent: resources are wrapped and enriched, dlt performs ingestion into the configured warehouse destination, and the transformation stage runs only when enabled. Settings are loaded from AXIOMATIC_* variables with CLI override support."
  - heading: Transformation strategy
    body: The transformation stage is dbt-first by design. Dependency graphing, model ordering, and tests stay with dbt, while orchestration stays with the engine. This avoids reimplementing mature dbt capabilities in engine internals and keeps CI execution straightforward.
  - heading: Warehouse strategy
    body: DuckDB and MotherDuck share a Duck-compatible base for common behaviour, with concrete adapters handling backend-specific validation. MotherDuck token handling remains environment-driven and is kept out of URI paths to reduce accidental credential exposure.
scopeSummary: Implemented scope includes REST and file-based ingestion paths, DuckDB-compatible warehouse execution, typed settings, and dbt-first transformation orchestration. Declared extension points remain for additional storage backends and warehouses, with explicit not-implemented signalling.
tradeOffs: The current design favours clear boundaries and predictable execution over broad backend coverage. This reduces ambiguity for early adopters, but it also means some integrations remain planned. Semantic and MCP standards are tracked as planned capability work until concrete implementation is published.
capabilityMaturity:
  ingestion: implemented
  transformation: implemented
  semantic_mcp: planned
evidence:
  - type: code
    claim: Engine purpose, boundaries, and current package structure
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/architecture.md
    status: implemented
  - type: code
    claim: Package install path, runtime configuration contract, and operational defaults
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/README.md
    status: implemented
  - type: code
    claim: Core execution flow and stage responsibilities across ingestion and transformation
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/architecture.md
    status: implemented
  - type: code
    claim: Implemented versus declared extension points and current runtime scope
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/architecture.md
    status: implemented
  - type: code
    claim: CLI-over-env runtime override pattern used by the project runner
    sourcePath: examples/fake_store/run_pipeline.py
    status: implemented
decisions:
  - id: ADR 001
    status: accepted
    summary: Keep shared Duck behaviour in a base class while isolating DuckDB and MotherDuck semantics in concrete adapters.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/001-warehouse-adapter-hierarchy.md
  - id: ADR 002
    status: accepted
    summary: Keep orchestration in engine, delegate model planning/testing to dbt, and enforce explicit warehouse compatibility checks.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/002-dbt-first-transformation-orchestration.md
  - id: ADR 003
    status: accepted
    summary: Treat SourceProtocol and ResourceProtocol as the stable boundary between domain extraction logic and engine orchestration.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/003-source-contract-boundary.md
  - id: ADR 004
    status: accepted
    summary: Inject a uniform _axiomatic_extracted_at_utc lineage field in BaseResource before load normalisation.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/004-axiomatic-extraction-metadata-injection.md
  - id: ADR 005
    status: accepted
    summary: Gate ingestion on landing outcome with explicit force_reload override for deterministic full reruns.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/005-pipeline-stage-gating-and-force-reload.md
  - id: ADR 006
    status: accepted
    summary: Use environment-first typed settings with explicit CLI override precedence for runtime control.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/006-environment-first-configuration-with-cli-overrides.md
  - id: ADR 007
    status: accepted
    summary: Resolve dbt project and profiles paths to absolute directories before subprocess invocation.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/007-dbt-project-and-profiles-path-resolution.md
  - id: ADR 008
    status: accepted
    summary: Establish a logging-first observability baseline for pipeline stage progress and diagnostics.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/008-observability-baseline-for-pipeline-execution.md
  - id: ADR 009
    status: accepted
    summary: Add typed resource load hints and map them in the source bridge with merge primary-key guardrails.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/009-resource-load-hints-contract-and-bridge-mapping.md
  - id: ADR 010
    status: accepted
    summary: Adopt hybrid schema evolution modes (auto, strict, discard) with source-bridge mapping to dlt semantics.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/010-hybrid-schema-evolution-policy.md
  - id: ADR 011
    status: accepted
    summary: Harden dbt runtime invocation with PATH fallback and MotherDuck token propagation safeguards.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/011-dbt-runtime-invocation-and-motherduck-token-propagation.md
  - id: ADR 012
    status: accepted
    summary: Standardise package quality gates with uv groups to protect distribution readiness before release.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/012-package-readiness-and-uv-quality-gate.md
  - id: ADR 013
    status: accepted
    summary: Introduce explicit source kinds and factory routing to keep project source definitions typed and extensible.
    sourcePath: https://github.com/axiomatic-bi/axiomatic-engine/blob/main/docs/adr/013-specific-source-kinds-and-source-factory-routing.md
---

This page anchors engine claims to architectural and ADR evidence, rather than framework-level marketing statements.

## Current measurable scope

- Protocol boundary areas: source, storage, warehouse, transformation.
- Implemented transformation backend: `dbt` (dbt-first orchestration path).
- Implemented warehouse adapters: `duckdb` and `motherduck` with shared Duck-compatible base behaviour.
- Runtime configuration is loaded from `AXIOMATIC_*` variables with CLI-over-env override support in project runners.
- Declared-but-unimplemented examples: storage `gcs`/`s3`, warehouse `bigquery`, and additional transformation backends.

## Operational behaviour already evidenced

- Ingestion supports per-resource `write_disposition` (`append`, `replace`, `merge`) and optional `primary_key`.
- Schema evolution policy supports `auto`, `strict`, and `discard`.
- Rerun semantics are explicit: `merge` for idempotent upsert behaviour, `replace` for deterministic snapshots, `append` for arrival history.
- dbt execution policy avoids internal `dlt.common.*` APIs to reduce compatibility risk across upstream changes.

```text
ResourceLoadHints
- write_disposition: append | replace | merge
- primary_key: optional, used by merge semantics
- schema_evolution_mode: auto | strict | discard
```

## Reproducibility boundary

Engine orchestration is exercised here through a project runner rather than a standalone engine CLI.  
Future semantic and MCP orchestration standards remain explicitly `planned` until implementation artefacts are published.
